---
context: default
phase: story-1.3-ios-gps-capgo-swap
task: 1
total_tasks: 8
status: paused_awaiting_external
last_updated: 2026-05-12T10:17:56-03:00
---

# Story 1.3 — iOS GPS Plugin Swap — Pausa pra aguardar build + teste device

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Truthy proxy do `Capacitor.registerPlugin()` | Plugin proxy é truthy mesmo sem código nativo na plataforma — fez Story 1.1 nunca rodar no iOS | blocking | Sempre `Capacitor.getPlatform() === 'ios'` para diferenciar plataforma. NUNCA `!plugin`. |
| QA PASS sem teste runtime | Story 1.1 teve PASS em todos os AC mas código nunca rodou no device | blocking | Story 1.3 AC7 inclui teste device do Fernando como gate de `Done` |
| Console.log sem Mac é inviável | João não tem Mac/Xcode → console.log puro não dá pra ver | blocking | Story 1.3 AC1 usa Sentry (`logGpsDiagnostic`) que envia logs remoto |
| Android tocado em story iOS | Restrição absoluta — `app/android/**` proibido em qualquer story iOS | blocking | @qa valida via `git diff --stat app/android/` antes de verdict |
| `git add -A` em monorepo | Pai do diretório tem sibling `uhuru-os` com modificações alheias | advisory | Sempre `git add` por path específico |

<current_state>
Branch `feat/ios-gps-capgo-swap` @ `3a8b355` pushed para origin. Story 1.3 AC1 (diagnóstico runtime) implementado com Sentry. Aguardando ação humana: João dispara Codemagic build #28 → Fernando instala via TestFlight → dirige 2-3min → logs aparecem em sentry.io/issues filtro `category:gps-diag`.
</current_state>

<completed_work>

**Esta sessão (2026-05-12):**

1. ✓ Resume `/gsd-resume-work` — carregou HANDOFF.json anterior (Story 1.2 build #27 awaiting test)
2. ✓ Fernando reportou: build #27 falhou no device (zero pontos GPS)
3. ✓ Decisão: ativar Plano B (swap plugin)
4. ✓ Descoberta importante: projeto JÁ está em Capacitor 8.2.0+ (não 7 como handoff antigo dizia). Memory corrigida.
5. ✓ @sm draftou Story 1.3 com 8 ACs (`docs/stories/1.3.story.md`)
6. ✓ @po validou: NO-GO 8.5/10 → 2 fixes em AC6/AC7
7. ✓ @sm aplicou os 2 fixes (AC6 com comando manual @qa, AC7 com definição de "ponto GPS")
8. ✓ @po revalidou: GO 10/10, status `Ready`
9. ✓ @dev implementou AC1 — 3 logs `[GPS-DIAG]` em `GeoService.js` (commit `0d6aaf2`)
10. ✓ @devops pushou branch nova `feat/ios-gps-capgo-swap` para origin (com `-u`)
11. ✓ Descoberta crítica: console.log puro inviável (João sem Mac, Fernando leigo) — pivot pra Sentry
12. ✓ João criou projeto Sentry `transfer-online`, gerou DSN, adicionou `VITE_SENTRY_DSN` no Codemagic
13. ✓ @dev integrou Sentry (commit `3a8b355`): `@sentry/capacitor@4.0.0` + `@sentry/react@10.43.0`, wrapper `app/src/lib/sentry.js`, init em `main.jsx`, substituiu `console.log` por `logGpsDiagnostic`
14. ✓ @devops pushou commit Sentry para origin
</completed_work>

<remaining_work>

**Bloqueado em ação humana:**

- **[João]** Disparar Codemagic build #28 manualmente para branch `feat/ios-gps-capgo-swap` (env var `VITE_SENTRY_DSN` já configurada)
- **[Fernando]** Instalar via TestFlight, dirigir 2-3min (sem outra ação — Sentry captura automático)

**Pós-teste (decisão Fase 2):**

| Resultado no Sentry (`sentry.io/issues`, filtro `category:gps-diag`) | Decisão |
|---|---|
| `[GPS-DIAG] Callback disparou lat=X lon=Y` aparece | Cancelar swap. Bug está em buffer/backend. Investigar. |
| `[GPS-DIAG] Registrando addWatcher` aparece mas `Callback disparou` nunca | Confirma hipótese NonescapableTypes → prosseguir Fase 2 (AC2-AC8) |
| Nem `Registrando addWatcher` aparece | Permission denied ou init failed. Investigar Settings iOS antes de swap. |

**Se decisão = swap (Fase 2):**

- @dev AC2 — Remover `@capacitor-community/background-geolocation@1.2.26`, instalar `@capgo/background-geolocation@8.0.32`. API muda: `start(options, callback) / stop()` em vez de `addWatcher`. Diff completo na story.
- @dev AC2 — Remover patch `sed` em `codemagic.yaml` (@capgo tem `Package.swift` nativo Capacitor 8)
- @dev AC3-AC5 — Preservar selector fix + Always permission + filtro adaptativo
- @dev AC6 — Validar Android intocado (`git diff --stat app/android/`)
- @dev AC8 — Bump `1.0.4 → 1.0.5`, versionCode `5 → 6` em `build.gradle` E `Info.plist`
- @qa qa-gate
- @devops push + dispara Codemagic build #29 release
- Fernando testa AC7 (≥5 pontos foreground, ≥3 background — definição de "ponto GPS" na story)
- AC7 PASS → Status `Done` → Story 1.3 fechada
</remaining_work>

<decisions_made>

- **Plano B ativado:** swap plugin (motivo: Story 1.2 selector fix mergeado mas build #27 falhou no device com zero pontos)
- **Fase 1 antes da Fase 2:** AC1 diagnóstico antes do swap completo (custo baixo, mata ambiguidade do sintoma "zero pontos")
- **Capacitor 8 já instalado:** sem bump necessário (`@capacitor/core@^8.2.0` confirmado em `app/package.json`)
- **Hipótese de causa-raiz:** Capacitor 8.0.2+ moveu `call.getString/getBool/reject/resolve` para trás de feature flag experimental `$NonescapableTypes` (issue ionic-team/capacitor#8333) — `@capacitor-community` Plugin.swift usa esses métodos linhas 90/93/106/156/219. Callbacks falhariam silenciosamente.
- **Sentry escolhido pra captura remota:** João sem Mac → console.log Xcode inviável; Fernando leigo → screenshot in-app frágil; Base44 não expõe logs custom. Sentry tem dashboard web em tempo real, free tier 5k events/mês, SDK Capacitor maduro.
- **AC6 validação manual pelo @qa** (sem CI/CD gate neste projeto)
- **AC7 definição operacional "ponto GPS":** 1 callback com lat/lon/accuracy válidos = 1 ponto. Cada log `[GPS-DIAG]` lat=X lon=Y precisao=Z conta como 1.
</decisions_made>

<blockers>

- **Codemagic build #28** — depende de João disparar manualmente no web UI (Codemagic NÃO dispara automático no push, mesmo)
- **Fernando teste device** — depende de Codemagic terminar build + Fernando estar disponível
- **Sem CI/CD gate** — projeto não tem CI; @qa precisa rodar `git diff --stat app/android/` manualmente como primeiro passo do gate
</blockers>

## Required Reading (in order)

1. `docs/stories/1.3.story.md` — story atual com 8 ACs, decision tree do AC1, diff da API @capgo
2. `docs/stories/1.2.story.md` — predecessor (selector fix, ineficaz no device)
3. `docs/stories/1.1.story.md` — primeira tentativa (truthy proxy bug)
4. `.planning/HANDOFF.json` — estado estruturado machine-readable
5. `docs/research/ios-gps-research-{runtime,architect,analyst}.md` — pesquisa que embasou Plano B

## Infrastructure State

- **Git branch:** `feat/ios-gps-capgo-swap` @ `3a8b355` (pushed origin)
- **Base branch:** `release/1.0.4`
- **Remote:** https://github.com/vxavierr/transfer-online-app/tree/feat/ios-gps-capgo-swap
- **Sentry projeto:** `transfer-online` em sentry.io (conta `vxavierr`)
- **Sentry DSN:** `https://de399a815dfa493aae52b878b27bc288@o4509898497327104.ingest.us.sentry.io/4511376950558720`
- **Sentry env var:** `VITE_SENTRY_DSN` adicionada no Codemagic (João confirmou)
- **Sentry SDKs:** `@sentry/capacitor@4.0.0` + `@sentry/react@10.43.0`
- **Codemagic:** pipeline iOS funcional, **trigger manual** (não automático no push)
- **App Store Connect:** Apple ID `6763598494`, Bundle `com.transferonline.app`
- **Build alvo:** #28 (anterior #27 publicado mas falhou no device)
- **Team AIOX:** `story-1-3-ios-gps` ainda vivo (sm/po/dev/devops idle, pode reativar)
- **Uncommitted (orchestrator):** `docs/stories/1.2.story.md` modificado em sessão anterior, não relacionado a 1.3 — ignorar/revisar depois

## TODO no código (não bloqueante)

- `app/src/native/services/GeoService.js:88` — `setSentryUser` precisa do `user_id` do Base44. Comentário com TODO no local. Adicionar quando contexto de autenticação estiver acessível no GeoService.

<context>
A sessão começou como `/gsd-resume-work` carregando HANDOFF.json antigo (Story 1.2 build #27 awaiting Fernando test). Fernando testou e falhou — zero pontos GPS no iPhone. Ativou Plano B (swap plugin). Mid-execution descobri que o esquema de logs do AC1 era impraticável (João sem Mac) e pivotamos pra Sentry. Tudo está pronto pra Fernando testar o build #28 sem que ele precise fazer nada além de dirigir 2-3min. Logs aparecem automático no painel Sentry em tempo real.
</context>

<next_action>
**Próxima ação literal:**

1. **João, no Codemagic web UI:** Selecione app `transfer-online` → branch `feat/ios-gps-capgo-swap` → workflow iOS → **Start build** (será build #28)
2. Quando build terminar (~15-25 min), envie pro Fernando via TestFlight
3. Fernando: instala, dirige 2-3 min, fim
4. Abra https://sentry.io/issues/ → filtre por tag `category:gps-diag`
5. Analise resultado conforme tabela em `<remaining_work>` acima
6. Quando voltar, rode `/gsd-resume-work` — eu carrego este `.continue-here.md` e seguimos Fase 2 (swap) ou cancelo
</next_action>
